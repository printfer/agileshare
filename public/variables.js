// Application configuration
const PASSCODE_LENGTH = 4;
const ECDH_CURVE = "P-521"; // Can be "P-256", "P-384", or "P-521"

// Signal
const TYPE_ASK_KEY = 0;
const TYPE_REPLY_KEY = 1;

const INIT_FILE_TRANSFER = 0;

// Local secrets
let my_passcode; // User entered passcode
let my_room_id; // Generated by passcode with current time
let my_room_secret; // Generated by passcode with room id 

let my_keypair; // Local generated ECDH key pair

// Local files
let my_upload_candidate;

// Local elements
const pincode_confirm = document.querySelector("#pincode_confirm");

const drop_zone_display = document.querySelector("#drop_zone_display");

const upload_confirm = document.querySelector("#upload_confirm");
upload_confirm.firstChild.disabled = true;
const download_confirm = document.querySelector("#download_confirm");
download_confirm.firstChild.disabled = true;

